<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ridi.mybatis.mapper.MemberMapper">
	<insert id="insertMember" parameterType="MemberDto">
		INSERT INTO MEMBER VALUES(
									SEQ_MEMBER.NEXTVAL,
									#{name},
									#{RRN},
									#{id},
									#{password},
									#{email},
									#{gender},
									#{zipcode},
									#{address},
									#{hp},
									SYSDATE
		)
	</insert>
	<select id="getLoggedMember" resultType="MemberDto">
		SELECT * FROM MEMBER WHERE ID=#{id} AND PASSWORD = #{password}
	</select>
	<insert id="modifyMember" parameterType="MemberDto">
		UPDATE MEMBER SET PASSWORD = #{wishPassword}, EMAIL = #{email}, ZIPCODE = #{zipcode}, ADDRESS = #{address}, HP = #{hp} WHERE ID = #{id} AND PASSWORD = #{password}
	</insert>
</mapper>






