<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ridi.mybatis.QnaMapper">
	<insert id="insertQna" parameterType="QnaDto">
		INSERT INT QNA VALUES (
							SEQ_QNA.NEXTVAL,
							
		)
	</insert>
	<select id="getAllList" parameterType="HashMap" resultType="QnaDto">
		SELECT * FROM (SELECT B.*,ROWNUM AS NUM FROM ( SELECT * FROM REPLYBOARD ORDER BY REGROUP DESC, RELEVEL ASC ) B	) 
		WHERE NUM  <![CDATA[>=]]>  #{start} AND NUM  <![CDATA[<]]>  #{end}
	</select>
</mapper>




